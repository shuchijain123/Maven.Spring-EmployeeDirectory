package io.zipcoder.persistenceapp.Service;

import io.zipcoder.persistenceapp.Models.EmployeeEntity;
import io.zipcoder.persistenceapp.repositories.EmployeeRepository;
import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.*;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class EmployeeServiceTest {

    @Test
    public void create() {
//        EmployeeEntity employee= new EmployeeEntity();
//        EmployeeRepository mockrepo = mock(EmployeeRepository.class);
//        //when(mockrepo.save(employee)).thenReturn(new EmployeeEntity(1L));
//
//        EmployeeService service = new EmployeeService(mockrepo);
//
//        EmployeeEntity actual = service.create(employee);
//
//        Assert.assertEquals(1,actual.getEmployeeNumber(),0);
//    }
    }
    @Test
    public void update() {
    }

    @Test
    public void show() {
    }
}